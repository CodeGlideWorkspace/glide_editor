import React from 'react'
import { Number as GNumber } from 'remote:glide_components/FormBase'

function Number(props) {
  return <GNumber {...props} style={{ width: '100%' }} />
}

export default Number
