import { Input, TextArea, Switch } from 'remote:glide_components/FormBase'

import Number from './Number'

export default {
  input: Input,
  textarea: TextArea,
  number: Number,
  switch: Switch,
}
